# Armada

Armada reconciles a user-scoped fleet of local Vehicle daemons through the host operating system's native service manager.

## Status

The manifest-to-plan walking skeleton is under development. Linux systemd, macOS launchd, and Windows Task Scheduler strategies follow in the next phase.

## Development

Requires Node.js 22.

```bash
npm install
npm run check
```
