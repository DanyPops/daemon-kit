name: Publish vehicle-client-pi

# Trusted publishing (OIDC), same shape as every other publish-*.yml in this
# workspace. Configure the trusted publisher on npmjs.com: org=DanyPops
# repo=vehicle workflow=publish-vehicle-client-pi.yml

on:
  push:
    tags:
      - "vehicle-client-pi-v*"

permissions:
  id-token: write
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: oven-sh/setup-bun@v2

      - run: bun install
      # Full workspace build -- vehicle-client-pi depends on @danypops/vehicle-core
      # and @danypops/vehicle-server (secrets-backend-local.ts reads vault.ts directly).
      - run: bun run build
      - run: bun run --cwd packages/vehicle-client-pi test
      - run: bun run --cwd packages/vehicle-client-pi typecheck

      - uses: actions/setup-node@v6
        with:
          node-version: "24"
          package-manager-cache: false

      - run: npm install -g npm@latest && npm --version

      - run: npm publish --access public
        working-directory: packages/vehicle-client-pi
